функция input() - считывает ввод с консоли
функция int(x) преобразует число x к целому
функция str(x) преобразует число x к строке
функция round(x, 2) округляет число с плавающей точкой до 2 знаков после запятой

# создание переменных
x = 1   # x - это целое число 1
x = '1' # x - это строка в которой написано 1

# строки можно сравнивать
x = '1'
x == '1' # True
x == '1 ' # False
x == 1 # Ошибка 

# массив состоящий из двух элементов
array = [1, 2]

# массив все ещё состоит из двух элементов,
# просто теперь каждый элемент тоже состоит из двух элементов
array = [(1, 2), (3, 4)]

# конструкция
if x > 1:
    print("Да")  # срабатывает при истинности условия
else
    print("Нет")  # при лжи

# конструкция переберёт каждый элемент массива array и выведет его на экран
for i in array:
    i[0] # первый элемент
    i[1] # второй элемент
    print(i)

# так же for может перебрать строку
for i in '123':
   print(i)


Для решения достаточно добавить к существующему коду добавить счетчик и цикл for

array = [(1, 2), (3, 4)]
count = 0

for s, t in array:
    if s > t:
      count = count + 1


Для решения можно перебрать b

for b in range(1, 20):
    for con in '1121':
         if con == '1':
             ...
         else:
             ...

Если итоговое число нецелое, то можно использовать округление
round(x, 2)

Сравнение лучше проводить для строк, а не для чисел.


Для решения можно перебрать все возможные наборы команд.
for com1 in '12':
   if com1 == '1':
        ...
   else:
        ...

   for com2 in '12':
      ...

вложенных циклов максимум пять.

Если итоговое число нецелое, то можно использовать округление
round(x, 2)

Сравнение лучше проводить для строк, а не для чисел.
